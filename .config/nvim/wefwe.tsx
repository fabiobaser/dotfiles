// generate prime number

function generatePrimeNumber() {}

interface User {
  firstName: string;
  lastName: string;
}

const user: User = {
  firstName: "John",
  lastName: "Doe",
};

function getFirstAndLastItemOfArray(arr: Array<string>): [string, string] {
  return [arr[0], arr[arr.length - 1]];
}
